import { addQuestion } from 'src/domain/entities/template/logics/addQuestion'
import { createNew } from 'src/domain/entities/template/logics/createNew'
import { setAuthor } from 'src/domain/entities/template/logics/setAuthor'

export const templateEntityLogics = {
  addQuestion,
  createNew,
  setAuthor,
}
